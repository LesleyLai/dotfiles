[user]
        email = lesley@lesleylai.info
        name = Lesley Lai

[core]
        editor = emacs
	excludesfile = /home/lesley/.gitignore

[credential]
        helper = cache

[alias]
        graph = log --graph --oneline --boundary --decorate=short
        unstage = reset HEAD --

[status]
        short = true
